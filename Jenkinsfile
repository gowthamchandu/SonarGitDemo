pipeline {
    agent any
    stages {
        stage('SonarQube Analysis') {
            steps {
                echo 'Preparing report...'
                sh "./script-file.sh"
            }
        }
    }
}
